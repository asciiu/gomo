package constants

const (
	AccountInsufficient string = "insufficient" // account has insufficient balance to what we have committed
	AccountInvalid      string = "invalid"      // keys no longer work for account
	AccountUnverified   string = "unverified"   // account balances have not been verified yet
	AccountVerified     string = "verified"     // when account balances
)
